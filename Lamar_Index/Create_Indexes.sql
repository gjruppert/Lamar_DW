/*
    Index definitions for Lamar data warehouse (svo schema)
    Indexes are now created by load procedures (drop at start of full reload, create at end).
    This script is retained as a no-op stub for backward compatibility.
    Run load procedures to create indexes.
*/

SET ANSI_NULLS ON;
GO
SET QUOTED_IDENTIFIER ON;
GO

PRINT 'Create_Indexes.sql: Indexes are defined and created in load procedures. No action taken.';
GO
